general {
    colors = false
    interval = 5
    separator = ""
    # color_good = "#FFFFFF"
    # color_degraded = "#FFFFFF"
    # color_bad = "#FF0000"
}

order += "disk /"
order += "ethernet _first_"
order += "tztime local"

ethernet _first_ {
        # if you use %speed, i3status requires root privileges
        format_up = " %ip"
        format_down = " down"
}

tztime local {
        format = " %a, %B %d   %H:%M:%S"
}

disk "/" {
        format = " %avail"
}
